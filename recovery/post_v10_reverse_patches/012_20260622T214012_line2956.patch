diff --git a/biomechanics_env.py b/biomechanics_env.py
--- a/biomechanics_env.py
+++ b/biomechanics_env.py
@@ -271,7 +271,12 @@
     BVH_LOCOMOTION_GATE_FLOOR = 0.2
-    BVH_BOOTSTRAP_GAIT_SCALE = 0.8
-    BVH_BOOTSTRAP_PROGRESS_SCALE = 0.25
+    BVH_BOOTSTRAP_GAIT_SCALE = 1.1
+    BVH_BOOTSTRAP_PROGRESS_SCALE = 0.45
+    BVH_REG_SWING_DRAG_WEIGHT = 0.20
+    BVH_REG_SWING_CONTACT_WEIGHT = 0.25
+    BVH_REG_CLEARANCE_DEFICIT_WEIGHT = 0.15
+    BVH_REG_DOUBLE_CONTACT_WEIGHT = 0.25
+    BVH_REG_DOUBLE_SUPPORT_DRAG_WEIGHT = 0.15
 
     def __init__(
-        self,
+        self,
         env_version: str = "standard",
@@ -1643,7 +1648,8 @@
             + 0.25 * jp.tanh(terms["foot_slip"])
-            + 0.35 * terms["swing_drag"]
-            + terms["swing_contact"]
-            + 0.35 * terms["clearance_deficit"]
-            + terms["double_contact"]
-            + 0.25 * jp.tanh(terms["double_support_drag"])
+            + self.BVH_REG_SWING_DRAG_WEIGHT * terms["swing_drag"]
+            + self.BVH_REG_SWING_CONTACT_WEIGHT * terms["swing_contact"]
+            + self.BVH_REG_CLEARANCE_DEFICIT_WEIGHT * terms["clearance_deficit"]
+            + self.BVH_REG_DOUBLE_CONTACT_WEIGHT * terms["double_contact"]
+            + self.BVH_REG_DOUBLE_SUPPORT_DRAG_WEIGHT
+            * jp.tanh(terms["double_support_drag"])
             + 0.35 * jp.tanh(terms["height"])
